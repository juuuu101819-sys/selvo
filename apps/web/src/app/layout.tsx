import './globals.css';

/**
 * Root shell. `<html lang>` lives on the `[locale]` layout so it tracks the negotiated locale.
 */
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
